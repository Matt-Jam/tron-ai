[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tron-ai"
version = "0.1.0"

[project.scripts]
tron-ai = "tron_ai.main:run"

[tool.setuptools.packages.find]
where = ["src"]